import { WorldIdReturn } from '@/components/WorldIdReturn'

export const metadata = { robots: { index: false, follow: false }, referrer: 'no-referrer' as const }

export default function WorldIdReturnPage() {
  return <WorldIdReturn />
}
